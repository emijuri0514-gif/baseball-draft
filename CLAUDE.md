# このリポジトリについて

AI対戦型のNPBドラフト＆スタメン編成シミュレーター（単一HTMLの静的サイト、ビルド不要）。
GitHub Pagesでそのまま公開している。主なファイル:

- `index.html` — ゲーム本体
- `howto.html` — 遊び方・ルール説明
- `players.html` — 選手名鑑（`index.html`の`initialDatabase`をfetchして表示するため、選手データはここには複製しない）
- `changelog.html` — 更新履歴
- `devblog.html` — 開発ブログ（連載形式）

## 更新のたびに必ずやること

ゲームの挙動・見た目・ロジックに関わる変更（バグ修正、AIロジック調整、UI変更、新機能追加など）を行ったら、
コードの変更だけで終わらせず、**必ず以下の2つも同じタイミングで更新する**:

1. **`changelog.html`**: `<ol>`の一番上（最新）に、変更内容を1〜2文で簡潔にまとめた`<li>`を追加する。
   既存のエントリと同じ見た目（タイムラインのドット＋見出し＋説明文）に合わせる。

2. **`devblog.html`**: 目次（`<nav>`内の`<ol>`）に新しい回を追加し、本文にも「第◯回：〜」という
   新しい`<article>`を追加する。**既存の回と同じトーンで書くこと**:
   - 一人称（僕）の日記っぽい自然な文体。堅い説明文やプレスリリース調にしない
   - 「〜という報告が来た」「気づいたら〜だった」のように、何が起きて・何に気づいて・どう直したか、
     という経緯を具体的に語る（抽象的な要約で済ませない）
   - 実際の数値・選手名・スクリーンショットで気づいた話など、具体性のあるディテールを入れる
   - 自分のミスや遠回りも隠さず書く（例: 係数の平均がズレて別の偏りを生んだ、など）
   - 「〜してみた」「〜だと思う」など、断定しすぎない自然な語尾を混ぜる
   - 長さは既存の回と同程度（2段落前後）でよい

どちらのページも見た目のトーン（ダークネイビー背景、Tailwind CDN、Noto Sans JP）を崩さないこと。

軽微な文言修正やタイポ直しなど、ユーザーに変更として認識されないレベルのものまで律儀に書く必要はない。
「何かが変わった・直った・追加された」と言えるレベルの変更が対象。
