# このリポジトリについて

AI対戦型のNPBドラフト＆スタメン編成シミュレーター（単一HTMLの静的サイト、ビルド不要）。
GitHub Pagesでそのまま公開している。主なファイル:

- `index.html` — ゲーム本体
- `howto.html` — 遊び方・ルール説明
- `players.html` — 選手名鑑（`index.html`の`initialDatabase`をfetchして表示するため、選手データはここには複製しない）
- `changelog.html` — 更新履歴
- `devblog.html` — 開発ブログ（連載形式）
- `lottery.html` / `lottery-big.html` / `lottery-shop.html` — 「野球くじ(β)」機能（予想くじ・非予想系ロトくじ・ポイント交換ショップ）。
  ドラフト本体とは別のFirebaseプロジェクトを使っており、`js/lottery-core.mjs` / `js/big-lottery-core.mjs` / `js/shop-items.mjs` に
  シミュレーション・オッズ計算・配当ロジックを純粋関数として分離している（元は`baseball-lottery-game`という別リポジトリで
  試作したものを、見た目とナビゲーションだけこのサイトに合わせて移植した）。ドラフト側のマルチプレイ機能（`rooms`コレクション）とは
  完全に独立しており、認証方式も異なる（ドラフト側はlocalStorageのclientId、くじ側はFirebase Anonymous Auth）。

## 更新のたびに必ずやること

ゲームの挙動・見た目・ロジックに関わる変更（バグ修正、AIロジック調整、UI変更、新機能追加など）を行ったら、
コードの変更だけで終わらせず、**必ず以下の2つも同じタイミングで更新する**:

1. **`changelog.html`**: `<ol>`の一番上（最新）に、変更内容を1〜2文で簡潔にまとめた`<li>`を追加する。
   既存のエントリと同じ見た目（タイムラインのドット＋見出し＋説明文）に合わせる。

2. **`devblog.html`**: 目次（`<nav>`内の`<ol>`）に新しい回を追加し、本文にも「第◯回：〜」という
   新しい`<article>`を追加する。**既存の回と同じトーンで書くこと**:
   - 一人称（僕）の日記っぽい自然な文体。堅い説明文やプレスリリース調にしない
   - 「〜という報告が来た」「気づいたら〜だった」のように、何が起きて・何に気づいて・どう直したか、
     という経緯を具体的に語る（抽象的な要約で済ませない）
   - 実際の数値・選手名・スクリーンショットで気づいた話など、具体性のあるディテールを入れる
   - 自分のミスや遠回りも隠さず書く（例: 係数の平均がズレて別の偏りを生んだ、など）
   - 「〜してみた」「〜だと思う」など、断定しすぎない自然な語尾を混ぜる
   - 長さは既存の回と同程度（2段落前後）でよい

どちらのページも見た目のトーン（ダークネイビー背景、Tailwind CDN、Noto Sans JP）を崩さないこと。

軽微な文言修正やタイポ直しなど、ユーザーに変更として認識されないレベルのものまで律儀に書く必要はない。
「何かが変わった・直った・追加された」と言えるレベルの変更が対象。

## 過去にハマった実装上の注意点（再発防止）

コード中の`★`コメントに経緯が詳しく書かれているが、特に同じ問題を繰り返しやすいものをここに抽出しておく。
新しい画面・スタイルを追加するときは先にこれを確認すること。

- **`justify-center` + `overflow-y-auto`は中身がビューポートより高いと上端が見切れる**（Pixel等の縦長でない端末で発生）。
  中央寄せしたいラッパーには`justify-center`ではなく`my-auto`を使う（auto marginは入りきらない時は0に潰れるため、
  どんな高さでも上端からスクロールして辿り着ける）。
- **`position: fixed` + `overflow-y-auto`な要素に直接背景画像を敷くと、スクロールしても背景だけ動かない**
  （背景が要素自身のボックス基準で固定されるため）。背景はfixedな親ではなく、中身を包む内側の
  通常フローのブロック（`stadium-bg`/`lottery-bg`/`lineup-bg`など）に付けること。
- **Tailwindのユーティリティクラス（`.fixed`など）で位置指定済みの要素に、独自CSSクラスで`position`を
  再宣言しない**。同じ特異性のクラス同士はスタイルシート内の後勝ちになり、意図せずTailwind側に
  上書きされて`position:fixed`が効かなくなることがある。
- **flexアイテムの上に`::after`などの疑似要素を重ねて使うときは、実コンテンツ側に明示的に
  `position: relative; z-index: 1;`を付ける**。flexアイテムは`z-index: auto`でも
  `position: relative; z-index: 0`相当で描画されるため、後から生成される`::after`が実コンテンツの
  上に乗ってしまうことがある（`::before`だけの場合は実コンテンツが後から描画されるので問題にならない）。
- **AI採点・AIドラフト評価の重み（`BATTER_TOOL_WEIGHT`や`ORDER_WEIGHTS`など）を調整するときは、
  理論値だけで判断しない**。Playwrightで実際に12球団分のドラフト・スタメン編成をシミュレーションし、
  特定の選手タイプ（5ツール型・長距離砲など）に偏っていないかを検証してから確定する。
- **選手データを新規追加するときは、`id`に週刊ベースボールONLINE（`sp.baseball.findfriends.jp`）の
  実在の選手ページIDをそのまま使う**。仮の連番などを入れると「選手詳細を見る」リンクが壊れる
  （過去に82人分やってしまい、後から実IDへの差し替えが必要になった）。
